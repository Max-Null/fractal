# 设置页重构-设计方案

> 版本：v1.1 ｜ 状态：已实现（2026-08-14 提交 5884e6c）｜ 读者：技术团队
> 关联：docs/设计/设置页设计方案.md（v1.0，数据模式开关，现状已过时）｜ docs/设计/模型配置-设计方案.md（v1.2，槽位制 + 真相源原则）
> 镜像：docs/原型/设置页原型文档.md（旧版，仅覆盖数据模式开关时代，未同步本次重构）

## 一、背景与目标

设置页（SettingsPanel.vue，1042 行）自迁移以来经多轮迭代（DeepSeek 专属重写、权限 6→4、数据模式开关、kimi key、消息排布、昵称头像、轻量模型、日志级别、预置技能），从未做整体梳理。当前问题：

1. **布局混乱**：三栏 `flex-wrap` 平铺（每栏固定 300px），「引擎设置」里混了 API 配置与会话参数，「高级设置」折叠区塞了重要性差异大的项。
2. **技术债**：8 段几乎重复的自定义下拉（语言/主题/字号/模型/权限/思考深度/排布/日志级别/轻量模型）；大量 emoji 当图标（📋🔒✏️⚡🐢🧠🚀），违反项目「图标一律用 lucide-vue-next」规范。
3. **功能缺失**（用户明确提出的 6 项）：思考过程节点开关、图片头像、默认主 Agent、子 agent 模型独立配置、系统通知、高级设置样式整理。

**目标**：布局重构（左侧导航 + 右侧单列）+ 补 6 项功能 + 清理技术债（抽通用组件 + 换 lucide）。

## 二、现状盘点

### 2.1 字段全集（23 个）

| 字段 | 存储 | 现状位置 |
|------|------|---------|
| apiKey（DeepSeek key） | provider-configs.json | 引擎设置 |
| kimiApiKey（多模态 key） | provider-configs.json | 引擎设置 |
| baseUrl | provider-configs.json | 引擎设置 |
| model（主模型） | provider-configs.json | 引擎设置 |
| contextLimit | SQLite | 引擎设置 |
| optimizeApiUrl（聊天 API 地址） | localStorage+SQLite | 引擎设置 |
| cwd（工作目录） | localStorage+SQLite | 工作目录区 |
| locale/theme/fontSize | localStorage+SQLite+settings.json | 界面设置 |
| permissionMode/planMode/autoMode（权限） | localStorage+SQLite | 界面设置 |
| effort（思考深度） | localStorage+SQLite | 界面设置 |
| messageLayout | localStorage+SQLite+settings.json | 界面设置 |
| nickname/avatar（emoji） | localStorage+SQLite+settings.json | 界面设置 |
| smallModel（轻量模型） | settings.json | 高级设置 |
| dataMode | settings.json | 高级设置 |
| opencodePath/logLevel/presetSkillsEnabled | settings.json | 高级设置 |
| currentAgent（主 agent） | localStorage | **无入口** |

### 2.2 已有设计约定（必须遵守）

- **真相源原则**（模型配置 v1.2）：OC 配置加载顺序 = 全局 opencode.json → 项目 opencode.json → **最后 agents/*.md**。子 agent 模型真相源是 `agents/*.md` frontmatter 的 `model` 行，GUI 是它的可视化编辑器；**写 opencode.json 的 agent 段无效**（被 .md 覆盖）。
- **槽位制**（模型配置 v1.2 + 契约）：`applyModelAliases` 按 `agents-manifest.json` 契约的槽位替换 .md 的 model 行。
- **配置分层**（settings.ts）：第 1 层 DEFAULT_SETTINGS 默认值 / 第 2 层 settings.json / 第 3 层密钥（provider-configs.json）。`parseAndValidate` 只保留 DEFAULT_SETTINGS 已知 key——**新字段必须加入 DEFAULT_SETTINGS + settings.schema.json，否则被丢弃**。
- **ui.* 三写**：UI 偏好同时写 localStorage + SQLite + settings.json。

## 三、关键设计决策

| 编号 | 决策 | 理由 |
|------|------|------|
| **D1 布局** | 左侧导航（6 tab）+ 右侧单列表单 | 功能 25+ 个，平铺必然爆炸；侧边栏可扩展、分类彻底（用户已拍板） |
| **D2 组件抽取** | 抽 4 个通用组件：SettingsSelect / SettingsInput / SettingsToggle / SettingsSection | 消灭 8 段重复下拉 + 统一视觉 |
| **D3 图标** | emoji 全部换 lucide-vue-next | 项目图标规范（开发规范四） |
| **D4 思考节点开关** | 新增 `ui.showThinking`（默认 true）；`v-show` 控制时间线 thinking 节点显隐，thinking 数据不删 | 用户确认：现有节点已是可展开一条，开关仅控制显隐，切回即恢复 |
| **D5 头像** | emoji 沿用 `ui.avatar`；新增 `ui.avatarImage` 存图片文件名；图片存 `<userData>/avatar/` | 图片+emoji 都要（用户拍板） |
| **D6 头像 IPC** | 新增 `avatar:pick`（选图复制）/ `avatar:clear`（删图） | 图片文件由主进程管理，渲染进程不直接持文件路径 |
| **D7 头像渲染** | 有图片显示 `<img>`，否则 emoji；清除图片回退 emoji | 字段分离，互不干扰 |
| **D8 默认主 Agent** | currentAgent 下拉（7 agent），补设置页入口 | 字段已存在默认「双星」，仅缺 UI |
| **D9 子 agent 覆盖表** | 新增 `agentModelOverrides`（settings.json，`Record<agent名, 模型全名>`，默认 `{}`） | 用户意图存 settings.json（主进程 applyModelAliases 需读） |
| **D10 覆盖优先级** | applyModelAliases：有合法覆盖 → 用覆盖值；无覆盖 → 槽位默认 | 「不设置显示默认」的落地 |
| **D11 候选按能力限定** | 文本 agent（双星/工匠/参谋/军师/助理）选 flash/pro；侦查兵选 ds-anthropic 端点；制图师选 kimi-k3 | 防呆：配错模型导致图像/联网能力静默失效（用户拍板） |
| **D12 「跟随默认」语义** | 从 overrides 删该 agent 条目 → 回到槽位默认 | 覆盖是增量，删除即还原 |
| **D13 通知 IPC** | 新增 `notification:show`（title/body） | Electron Notification 由主进程发 |
| **D14 通知配置** | `ui.notifications`（enabled + replyDone/engineError/permissionPending/subtaskDone），进 settings.json（ui.* 三写） | 所有配置统一进 settings.json，不搞 localStorage 特例（用户拍板） |
| **D15 通知默认** | 全局默认开 + 回答完成/引擎异常/权限请求 3 场景默认开，子任务完成默认关 | 用户 2026-08-14 定案：默认开启「除子智能体通知外」的三种通知（v1.1 修订，原 v1.0 为全局关） |
| **D16 引擎异常通知** | 由主进程（server-manager exit 回调）直接 Notification，不走前端 | 引擎异常时前端可能已断连 |
| **D17 高级设置** | 折叠区 → 独立「高级」tab，字段不变 | 样式整理，非字段变更 |
| **D18 key 变更引导弹窗** | key 输入框 blur 检测变化 → 自动串行执行「测试连接 → 保存并重启引擎」，弹窗只做进度/结果反馈（testing→saving→success 自动关 / error 重试），testing/saving 阶段禁止关闭（ModalShell `closable=false`） | 小白不懂「重启引擎」；页面手动「测试连接/保存并重启」按钮移除，弹窗成为唯一入口（用户拍板，v1.1 新增） |
| **D19 默认主 Agent 范围** | 下拉仅 3 项：双星 / build / plan（`PRIMARY_AGENTS`） | 用户 2026-08-14 反馈「为啥不是只有双星、build、plan」，对齐 InputBar 的 AGENT_OPTIONS（v1.1 修订，原 v1.0 为 7 agent） |
| **D20 头像协议** | 自定义 `avatar://` 协议（standard/secure/bypassCSP），仅暴露 `<userData>/avatar` 目录；文件名白名单 `/^avatar\.(png|jpg|jpeg|webp)$/` 主进程+渲染双保险防路径遍历 | `file://` 被 webSecurity 拦截（Not allowed to load local resource）；不开全局 webSecurity:false（v1.1 修订，原 v1.0 为 getPath/file:// 方案） |
| **D21 头像图标** | 头像候选 = 24 个 lucide 图标（`avatar-icons.ts`，科技科幻风格），非 emoji；`ui.avatar` 存图标 id | 用户拍板「换 lucide 图标」+「科技科幻风格」；emoji 违反复用规范（v1.1 修订） |
| **D22 轻量模型默认** | `smallModel` 默认 `deepseek/deepseek-v4-flash`（原跟随主模型 ''）；`''` 仍可显式选跟随主模型 | 用户 2026-08-14 定案「轻量模型默认用 flash」；引擎侧 resolveSmallModel 缺失字段本就兜底 flash，UI 默认对齐（v1.1 新增） |

## 四、布局与分类

### 4.1 页面结构

```
┌──────────┬─────────────────────────────┐
│ 通用      │  [SettingsSection 分组标题]  │
│ 模型与API │  label         [控件]       │
│ AI 行为   │  label         [控件]       │
│ 通知      │  ...                        │
│ 高级      │                             │
│ 关于      │                             │
└──────────┴─────────────────────────────┘
```

### 4.2 六 tab 内容

| Tab | 字段 |
|-----|------|
| 通用 | 语言、主题、字号、消息排布、昵称、头像（lucide 图标 24 个 + 图片上传） |
| 模型与 API | API Key（含余额 + blur 引导弹窗）、kimi Key（含余额）、baseUrl、主模型、上下文窗口 |
| AI 行为 | 默认主 Agent（双星/build/plan）、子 agent 模型（7 个）、权限模式、思考深度、思考节点开关、轻量模型（含 desc） |
| 通知 | 全局开关 + 4 场景开关（全局关时场景禁用） |
| 高级 | 数据模式、OC 路径、日志级别、预置技能、JSON 编辑器 |
| 关于 | 版本、引擎版本、预置版本、更新日志（右对齐） |

> v1.1 修订：移除「聊天 API 地址」（optimizeApiUrl 死字段）、「工作目录」（header 工作区管理更完整）、「重开引导」（对已配置 key 用户无实际用途）。

### 4.3 通用组件接口

| 组件 | Props（核心） | 说明 |
|------|--------------|------|
| SettingsSelect | `label / modelValue / options[{value,label,desc?,icon?}] / disabled? / desc?` | 下拉，含 Transition 动画，选中高亮 accent-glow；`desc` 为字段级描述（label 下方灰字，v1.1 新增，轻量模型用） |
| SettingsInput | `label / modelValue / type? / placeholder? / readonly?` + slot 后缀按钮 | 输入框，统一样式 |
| SettingsToggle | `label / desc? / modelValue / disabled?` | 开关，accent 开启态；label 灰字 text-secondary 与字段 label 统一（v1.1） |
| SettingsSection | `title` + slot | 分组标题容器 |

## 五、新功能设计

### 5.1 思考过程节点开关（D4）

- **字段**：`ui.showThinking: boolean`（默认 `true`）
- **存储**：ui.* 三写（localStorage + SQLite + settings.json，跟随 messageLayout 模式）
- **生效**：时间线组件（node-timeline）的 thinking 节点加 `v-show="settings.showThinking"`；开关是纯前端显隐，thinking 数据（contentBlocks）不动，切回立即恢复
- **入口**：AI 行为 tab

### 5.2 图片头像（D5/D6/D7/D20/D21）

- **字段**：`ui.avatar`（lucide 图标 id，24 个候选）+ `ui.avatarImage`（图片文件名，新增）
- **存储**：图片文件 `<userData>/avatar/avatar.{ext}`；`ui.avatarImage` 存文件名（三写）
- **IPC**：
  - `avatar:pick`：openDialog 选图 → 校验扩展名（png/jpg/jpeg/webp）→ 复制到 avatar 目录（统一命名 `avatar.{ext}`，覆盖旧图）→ 返回文件名
  - `avatar:clear`：删除 avatar 目录文件 → 清 `ui.avatarImage`
- **URL 加载（v1.1 修订）**：自定义 `avatar://` 协议（`avatar-protocol.ts`）——`registerSchemesAsPrivileged`（standard/secure/bypassCSP/supportFetchAPI，app ready 前）+ `protocol.handle`（app ready 后、任何窗口创建前）。handler 用「去首尾斜杠」切片提取文件名（兼容 `avatar:///x.png` 与 `avatar://x.png/`），白名单 `/^avatar\.(png|jpg|jpeg|webp)$/` 校验，decodeURIComponent 非法转义兜底 404
- **cache-busting（v1.1 新增）**：`pickAvatar` 成功后 `avatarRevision++`，URL 带 `?v=${revision}`——统一命名覆盖旧图后文件名不变，版本号强制 Chromium 重新加载
- **上传器交互（v1.1）**：avatar-uploader 无图=虚线圆形占位框，有图=预览 + hover 左右分半大热区（更换/清除）；图标候选 8 列 56px 圆形网格
- **渲染**：消息区/顶栏头像组件三级回退：图片 → lucide 图标 → 字符兜底（兼容旧 emoji）

### 5.3 默认主 Agent（D8/D19）

- **字段**：`currentAgent`（现有，localStorage，默认「双星」）
- **入口**：AI 行为 tab，SettingsSelect，选项 = **3 个主 agent（双星/build/plan，`PRIMARY_AGENTS`）**——子 agent（工匠/军师等）不可作主 agent，SUBAGENT_ORDER 仅用于「子 agent 模型」行（v1.1 修订，原为 7 agent）
- **生效**：currentAgent 已由 chat:sendMessage 的 `agent` 参数消费，改设置即改新会话默认；不重启

### 5.4 子 agent 模型独立配置（D9-D12，后端改动最大）

#### 5.4.1 覆盖表存储

- settings.json 新增字段 `agentModelOverrides: Record<string, string>`（默认 `{}`）
- 示例：`{ "工匠": "deepseek/deepseek-v4-pro", "参谋": "deepseek/deepseek-v4-flash" }`
- key = agent 名（去 .md），value = 模型全名（provider/model 格式）

#### 5.4.2 候选范围（按能力限定）

| Agent | 槽位 | 「跟随默认」值 | 可选候选 |
|-------|------|---------------|---------|
| 双星 | high | 主模型 | deepseek-v4-flash / pro |
| 工匠 | low | 轻量模型 | flash / pro |
| 助理 | low | 轻量模型 | flash / pro |
| 参谋 | inherit | 继承主模型 | flash / pro |
| 军师 | inherit | 继承主模型 | flash / pro |
| 侦查兵 | anthropic | ds-anthropic/deepseek-v4-flash | ds-anthropic flash / pro |
| 制图师 | vision | moonshotai-cn/kimi-k3 | kimi-k3（唯一） |

> 注：参谋/军师现状为 inherit（无 model 行，继承主模型）。用户显式覆盖 → 写 model 行（从继承变固定）；「跟随默认」→ 不写（保持继承）。

#### 5.4.3 applyModelAliases 改造（preset.ts）

现状逻辑（`electron/main/preset.ts:424`）：按槽位 `slotValues` 替换 .md 的 model 行，inherit 跳过。

改造后：

```
1. 读 settings.json 的 agentModelOverrides（fsp.readFile + JSON.parse，损坏兜底 {}）
2. for 每个 rule（契约 agents）:
   override = overrides[agent名] 且值在候选白名单内
   if override:
      value = override           // 用户显式覆盖（含 inherit 槽位也写 model 行）
   elif rule.slot === 'inherit':
      continue                   // 无覆盖的 inherit 保持继承
   else:
      value = slotValues[rule.slot]  // 槽位默认
   替换 model 行（值不变不写盘，保持幂等 + mtime 保护）
```

- **白名单校验**：候选范围表（5.4.2）硬编码于 preset.ts，非法覆盖值忽略（回退槽位默认）
- **inherit 覆盖**：被覆盖的 inherit agent 需写 model 行——`/^model:.*$/m` replace 在无 model 行文件上不匹配，需额外处理（无 model 行时在 frontmatter 追加一行）；实施时确认 frontmatter 结构再定

#### 5.4.4 联动触发

- `agentModelOverrides` 加入 settings.ts 的 `ENGINE_KEYS`（变更触发 `syncEngineConfig`）
- `syncEngineConfig` 在 ensureConfig 之后追加 `applyModelAliases(userDataDir)`（重新按覆盖表替换 .md）
- 前端选完模型 → 写 settings.json → 主进程联动 → 替换 .md → 下次会话生效（不重启 serve，与模型变更同语义）

#### 5.4.5 前端 UI

- AI 行为 tab「子 agent 模型」分组：7 行 SettingsSelect
- 每行：label = agent 名 + 槽位说明，选项 = 「跟随默认（当前值）」+ 候选模型
- 选「跟随默认」→ 从 overrides 删 key；选具体模型 → 写 key

### 5.5 系统通知（D13-D16，全新）

#### 5.5.1 配置与存储

- `ui.notifications`：`{ enabled: boolean, replyDone: boolean, engineError: boolean, permissionPending: boolean, subtaskDone: boolean }`
- 存储：进 settings.json（ui.* 三写：localStorage + SQLite + settings.json，跟随 messageLayout 模式）
- 默认（v1.1 修订）：`enabled=true` + 回答完成/引擎异常/权限请求 3 场景 `true`，子任务完成 `false`（原 v1.0 为全局关，opt-in）
- **disabled 联动**：全局关时 4 个场景开关禁用（`pointer-events:none` + 半透明）

#### 5.5.2 IPC

- `notification:show(title, body)`：主进程 `new Notification({title, body})`；仅 Windows 通知中心可用时弹出，异常吞掉（通知失败不阻断主流程）

#### 5.5.3 触发点

| 场景 | 触发位置 | 通道 |
|------|---------|------|
| AI 回答完成 | 前端：assistant 消息 isStreaming 由 true→false（消息流结束回调） | 前端检查开关 → notification:show |
| 引擎异常 | 主进程：server-manager exit 回调（非主动停止且非零退出） | 主进程直接 Notification（不经前端） |
| 权限请求待处理 | 前端：收到 permission 请求事件时 | 前端检查开关 → notification:show |
| 子任务完成 | 前端：subtask 事件流结束（task 子 agent 完成） | 前端检查开关 → notification:show |

### 5.6 API Key 变更引导弹窗（D18，v1.1 新增）

- **触发**：DeepSeek/Kimi key 输入框 blur，检测到与 `servedApiKeySnapshot`（挂载时快照）不同 → 自动启动流程
- **流程状态机**（KeyChangeDialog.vue）：`testing`（测试连接）→ 失败 → `error`（显示原因 + 重试/取消，不保存不重启）→ 成功 → `saving`（保存并重启引擎）→ `success`（✓ 1.2s 自动关闭）
- **不可打断**：testing/saving 阶段 ModalShell `closable=false`（ESC/遮罩/× 三通道禁用），防止「看似取消实则后台仍在保存重启」的状态不一致
- **移除页面手动按钮**：模型与 API tab 不再有「测试连接 / 保存并重启引擎」按钮，弹窗为唯一入口
- **保存**：deepseek → `saveCurrentConfig(true)`；kimi → `saveKimiKey(true)`（providerId=moonshotai-cn，keyChanged 恒 true 必重启）

### 5.7 稳定性修复（v1.1 新增，非原设计但影响设置页行为）

- **通知开关「多点几次才有效」根因**：`applySettingsJson` 对 notifications/agentModelOverrides 无条件整体替换引用 → deep watch（3 条写盘链均 `deep:true`）对引用替换必触发 → 写盘→广播→回写替换→再写盘无限循环（每 800ms 一次写盘风暴，UI 事件处理被拖慢）
- **修复**：同值不替换引用（`sameNotifications` 浅比较 / overrides 键值比较），值相同则保持原对象——循环断开
- **权限/思考深度写回**：planMode/autoMode/permissionMode/effort 并入 themeSyncTimer 防抖链（原只写 localStorage+SQLite，重启被 settings.json 旧值覆盖导致丢失）；`toSettingsPermissionMode` 四值映射
- **readServedApiKey**：saveProviderConfig 的 keyChanged 基准 = serve 实际加载的 key（`server-manager.getServedApiKey`，spawn 成功后从 opencode.jsonc/json 快照读取），而非 provider-configs.json 旧值（会被 store watch 防抖提前污染）——key 失效后「配置有效但不重启 serve」的修复核心

## 六、数据存储与 schema 变更

### 6.1 新增字段清单

| 字段 | 类型 | 默认 | 存储 | 是否进 settings.json |
|------|------|------|------|---------------------|
| ui.showThinking | boolean | true | 三写 | 是（+DEFAULT_SETTINGS +schema） |
| ui.avatarImage | string | '' | 三写 | 是（+DEFAULT_SETTINGS +schema） |
| agentModelOverrides | Record<string,string> | {} | settings.json | 是（+DEFAULT_SETTINGS +schema） |
| ui.notifications.* | object | v1.1：`{enabled:true, replyDone:true, engineError:true, permissionPending:true, subtaskDone:false}` | 三写 | 是（+DEFAULT_SETTINGS +schema） |
| smallModel | string | v1.1：`deepseek/deepseek-v4-flash`（原 ''） | settings.json | 是（+DEFAULT_SETTINGS +schema） |

### 6.2 schema 定义（settings.schema.json）

- `ui.showThinking`：`{ type: "boolean", default: true }`
- `ui.avatarImage`：`{ type: "string", default: "" }`
- `agentModelOverrides`：`{ type: "object", default: {}, additionalProperties: { type: "string" } }`（值白名单由 applyModelAliases 校验，schema 只约束字符串）
- `ui.notifications`：`{ type: "object", default: { enabled: true, replyDone: true, engineError: true, permissionPending: true, subtaskDone: false }, properties: { enabled: { type: "boolean" }, replyDone: { type: "boolean" }, engineError: { type: "boolean" }, permissionPending: { type: "boolean" }, subtaskDone: { type: "boolean" } } }`
- `smallModel`：`{ type: "string", enum: ["", "deepseek/deepseek-v4-flash", "deepseek/deepseek-v4-pro"], default: "deepseek/deepseek-v4-flash" }`

### 6.3 settings.ts 变更

- `DEFAULT_SETTINGS` 追加 4 个字段
- `ENGINE_KEYS` 追加 `agentModelOverrides`（触发引擎联动）
- `syncEngineConfig` 追加 applyModelAliases 调用

## 七、后端改动清单

| 文件 | 改动 |
|------|------|
| `electron/main/settings.ts` | DEFAULT_SETTINGS（含 smallModel/通知默认值）+ ENGINE_KEYS + syncEngineConfig 追加 applyModelAliases |
| `electron/main/settings.schema.json` | 字段定义 + 默认值（smallModel flash / 通知 3 场景开） |
| `electron/main/preset.ts` | applyModelAliases 支持 agentModelOverrides 覆盖 + 白名单 + inherit 覆盖写行 |
| `electron/main/ipc.ts` | avatar:pick / avatar:clear / notification:show / engine:testKimiConnection / kimi:getBalance；saveProviderConfig 用 getServedApiKey 判重启 |
| `electron/main/server-manager.ts` | 新增 readServedApiKey + getServedApiKey（v1.1） |
| `electron/main/avatar-protocol.ts`（新增） | avatar:// 协议注册 + handler（v1.1） |
| `electron/main/preload.ts`（ALLOWED_INVOKE） | 放行新 IPC 通道 |

## 八、前端改动清单

| 文件 | 改动 |
|------|------|
| `SettingsPanel.vue` | 整体重写为左侧导航 + 右侧单列；key blur 引导弹窗接线；移除手动测试/保存按钮；PRIMARY_AGENTS |
| 新增 `SettingsSelect.vue` / `SettingsInput.vue` / `SettingsToggle.vue` / `SettingsSection.vue` | 通用组件（SettingsSelect 含 desc prop） |
| 新增 `KeyChangeDialog.vue`（v1.1） | key 变更自动流程状态机弹窗 |
| `ModalShell.vue` | 新增 `closable` prop（false 禁 ESC/遮罩/×） |
| `stores/settings.ts` | 新增 showThinking / avatarImage / avatarRevision / notifications 字段；同值防循环；权限/effort 写回；smallModel 默认 flash；移除 optimizeApiUrl / resetOnboarding |
| `lib/electron-bridge.ts` | 新增 avatar / notification / kimi IPC 封装 |
| `lib/avatar-icons.ts`（新增） | 24 个 lucide 头像图标映射 |
| `composables/useAvatarImageUrl.ts`（新增） | avatar:// URL + ?v= cache-busting |
| 头像渲染组件 | 图片 → lucide 图标 → 字符三级回退 |
| 时间线组件（node-timeline） | thinking 节点 v-show |
| i18n（zh/en） | 新增所有文案 key（105=105 对齐） |

## 九、测试要点

| 层 | 用例 |
|----|------|
| preset | applyModelAliases 覆盖优先于槽位；白名单外忽略；inherit 覆盖写行；跟随默认不写；幂等（值不变不写盘） |
| settings | 新字段默认值 / parseAndValidate 保留 / schema default（通知 3 场景开、smallModel flash）/ 引擎快照联动触发 |
| server-manager | readServedApiKey 3 用例（jsonc 提取 / json 回退 / 兜底空串）（v1.1） |
| avatar-protocol | 协议注册特权 / 合法 200+MIME / 非法扩展名 403 / 路径遍历 403 / 文件缺失 404 / 非法转义 404（v1.1，7 用例） |
| ipc | avatar:pick/clear 文件复制删除；notification:show 通道；kimi 测试连接/余额；keyChanged 基准=getServedApiKey |
| store | showThinking/avatarImage/notifications 持久化；子 agent 覆盖读写；**同值对象不替换引用（防循环）**；权限/effort 四值映射写回；smallModel 默认 flash |
| SettingsPanel | 6 tab 渲染 / 子 agent 下拉选中写覆盖 / 通知开关联动 / **key blur 弹窗全链路（success 自动关 / failure 重试 / kimi 链路）** |
| 回归 | 现有 1196+ 用例不回归（settings.test / ipc.test / preset.test；server-manager 真 spawn 用例环境 flaky 与改动无关） |

## 十、风险与边界

- **inherit 覆盖写行**：无 model 行的 .md 需在 frontmatter 追加 `model` 行——已实现（实施时确认 frontmatter 结构后定案）
- **覆盖表与槽位默认漂移**：主模型/轻量模型切换时，「跟随默认」的 agent 值跟着变（符合语义）；显式覆盖的 agent 值固定不变
- **mtime 保护**：applyModelAliases 幂等（值不变不写盘）保证 mtime 不被无谓更新，用户改过的 .md 不被预置升级冲掉（沿用现状）
- **通知权限**：Windows 首次通知需系统授权，失败静默（不阻断）；引擎异常通知依赖主进程存活（serve 崩溃 ≠ 主进程崩溃，主进程仍在可发通知）
- **头像图片安全**：只接受图片扩展名 + 复制到受控目录；avatar:// 协议主进程白名单 + renderer 白名单双保险（防路径遍历）；decodeURIComponent 非法转义兜底 404
- **key 自动流程竞态**：blur 触发流程进行中用户再改 key → 成功回调里比对 `keyChangeTarget` 才关闭，避免旧流程关掉新弹窗；testing/saving 阶段 closable=false 防打断
- **防循环副作用**：applySettingsJson 同值不替换引用——值不同时仍正常替换，功能无回归（循环断开后写盘频率恢复正常）

## 十一、变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0 | 2026-08-13 | 初稿：布局重构（左侧导航）+ 6 项新功能（思考开关/图片头像/默认 agent/子 agent 模型/系统通知/高级整理）+ 技术债清理 |
| v1.1 | 2026-08-14 | 实现后修订：通知默认 3 场景开；轻量模型默认 flash + desc；key 变更引导弹窗（D18）；主 agent 限定 3 项（D19）；avatar:// 协议 + cache-busting（D20/D21）；防循环/权限写回/readServedApiKey 修复；移除 optimizeApiUrl/工作目录/重开引导；字号 px→rem |
