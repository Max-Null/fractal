# 分形 — 待定问题清单

> 创建：2026-08-05 | 配套：方案 v1.12 + 原型 v0.23
> 用途：原型已覆盖全部模块，但部分模块"UI 已设计、底层机制未定"。本清单是开发前的对齐依据——每项标注影响范围与解决路径，解决后回填结论并关闭。
> 更新：2026-08-05 P1-P3、P9-P13 用户已拍板；P4-P8 待阶段 0 实测。

## 优先级说明

- 🔴 **数据通道缺失**：模块设计了，但数据来源/通信机制是空白——必须补设计
- 🟡 **serve 行为待实测**：依赖 OC serve 实际行为，进阶段 0 实测
- 🟠 **产品决策待拍板**：需要用户确认的交互/生效语义

---

## 🔴 第一组：数据通道（3 项）——✅ 已拍板

### P1. 记忆面板 ↔ 插件数据通道 ✅ 已定

| 维度 | 内容 |
|------|------|
| 已设计 | 三层记忆浏览（pending/auto/suggest）、BM25 搜索、确认 pending→auto、手动增删（原型 v0.3+ 右侧面板） |
| **用户拍板（2026-08-05）** | **实时监听刷新**：插件写记忆/状态文件 → GUI 文件监听（chokidar/fs.watch）实时更新 |
| 技术方案 | ①插件（OC 进程内 fractal.ts）定期 dump 记忆索引到独立配置目录；②GUI 主进程监听该目录文件变化实时刷新面板；③「确认/删除」由 GUI 主进程直接改文件（插件读取时自然感知），验证插件 5s 缓存刷新机制 |
| 状态 | ✅ 方向已定，阶段 1 实现时细化 |

### P2. 状态面板 ↔ 插件运行时状态 ✅ 已定

| 维度 | 内容 |
|------|------|
| 已设计 | 触发线监控卡片：断言计数（分级）、编辑审查队列、无反馈环轮次、知识提取状态（原型 v0.12 状态 tab） |
| **用户拍板（2026-08-05）** | **实时监听刷新**（与 P1 同通道） |
| 技术方案 | 插件在状态变更时写状态文件（`.fractal-state.json`），GUI 文件监听实时刷新；或复用 serve SSE 事件流透传插件事件（待实测事件类型，作为备选） |
| 状态 | ✅ 方向已定，阶段 1 实现时细化 |

### P3. 触发线警告的 GUI 联动 ✅ 已定

| 维度 | 内容 |
|------|------|
| 已设计 | 状态卡片展示"已注入温和提醒"等（原型 v0.12） |
| 结论 | 跟随 P2 的状态文件通道；聊天流内的警告文本由事件流自然呈现（无额外设计） |
| 状态 | ✅ 跟随 P2 |

---

## 🟡 第二组：serve 行为待实测（5 项）

| # | 疑问 | 未知点 | 影响范围 | 解决路径 |
|---|------|--------|---------|---------|
| P4 | 权限-全自动模式 | `--auto` 在 serve 模式是否生效（当前仅确认 CLI 启动参数） | 权限模式翻译（方案 3.5） | 阶段 0 实测（附录 B serve 轨）；兜底：permission 全 allow。2026-08-06：`--auto` 已可透传 serve 启动（S0 实测），生效性待 S6 |
| P5 | 提问弹窗（question 工具） | question 触发时通过什么通道推给客户端（permission 请求 / SSE 事件 / control 通道）？选项+自由输入的数据格式 | 提问弹窗（原型 v0.12） | 阶段 0 实测 question 工具触发流程。2026-08-06：serve 认证机制已实测（附录 C-10/C-11），question 通道待 S5/S6 |
| P6 | 附件/引用发送格式 | `POST /session/:id/message` 的 parts 是否支持文件内容/路径引用；附件 chips 语义 | 附件/引用机制（原型 v0.17） | 阶段 0 实测 prompt parts 结构 |
| P7 | 回答时间线阶段识别 | 消息 part 真实结构（text/thinking/tool）→ 「回应 vs 答案」区分规则（首个 text vs 末个 text）、tool running/completed 状态流转 | 回答流程时间线（原型 v0.7） | 阶段 0 实测事件序列 + fixture |
| P8 | 会话级切换的"配置变更"记录 | 模型/agent 切换后 GUI 插入"配置变更"虚拟消息的机制（写本地消息表？渲染层合成？） | 会话级选择器（原型 v0.19/0.20） | 设计决策：先按"本地合成虚拟消息"实现，实测消息流合并后确认 |

---

## 🟠 第三组：产品决策（5 项）——✅ 已拍板

| # | 疑问 | 拍板结论（2026-08-05） | 影响范围 |
|---|------|------------------------|---------|
| P9 | 配置生效时机 | **下次会话生效**（写 opencode.json 后对当前会话不生效，新建/重开会话时生效；方案 3.8.3 倾向一致） | 管理面板、权限模式、模型切换 |
| P10 | 上下文用量上限语义 | **保底基数**：优先自动获取模型上下文窗口，获取不到时用手动值兜底；占比用于触发自动压缩（与 cc-gui 的 CC 底座机制一致）；**已查证（2026-08-06）**：opencode-acp 支持 `acp.jsonc` 配置 `compress.maxContextLimit`（默认窗口 55%）/ `minContextLimit`（45%）/ `modelMaxLimits` 按模型覆盖；窗口取模型元数据 `limit.context`（serve 上报 deepseek 全系 1000000） | 上下文面板（v0.12） |
| P11 | 技能「运行」按钮 | **插入 /skill 命令**（对话中插入对应命令并发送，与 oc-plus 技能执行方式一致） | 管理面板技能 tab（v0.12） |
| P12 | 修改重发语义 | **填入输入框**（当前原型：原消息内容填入输入框，改完正常发送） | 消息操作（v0.15） |
| P13 | 记忆/状态展示位置 | **右侧 tab**（保持当前：文件/记忆/状态/计划/预置 + 预览列） | 右侧面板架构（v0.10+） |

**P10 衍生待查证项**：opencode-acp 是否支持配置上下文窗口/压缩阈值（查 ACP 文档/配置项）→ 结论回填 P10。

---

## 汇总与下一步

| 类别 | 数量 | 状态 |
|------|------|------|
| 🔴 数据通道 | 3（P1-P3） | ✅ 已拍板（实时监听刷新），阶段 1 细化实现 |
| 🟡 serve 待实测 | 5（P4-P8） | ⏳ 待阶段 0 实测（并入附录 B/C） |
| 🟠 产品决策 | 5（P9-P13） | ✅ 已拍板 |
| 衍生待查证 | 1（ACP 配置支持性） | ⏳ 查 opencode-acp 文档 |

**回填规则**：每项解决后在本清单标记 ✅ 并注明结论与日期；实测结论同时回填方案附录 C 与知识库。
